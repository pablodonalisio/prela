# local (default): Postgres on localhost + files on disk
# remote: shared development DB (credentials :development_db) + S3
DEV_DATA_SOURCE=local

# Local Postgres via docker compose (postgres-db on port 5432)
# DATABASE_USERNAME=root
# DATABASE_PASSWORD=password
