<div id="<%= dom_id client %>">
  <p>
    <strong>Name:</strong>
    <%= client.name %>
  </p>

  <p>
    <strong>Avatar:</strong>
    <%= link_to client.avatar.filename, client.avatar if client.avatar.attached? %>
  </p>

</div>
