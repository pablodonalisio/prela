<%# The remote_modal frame id and the remote_modal_body element id are part of
    the contract with the form_update turbo streams and the request specs, so
    they must not be renamed. %>
<%= turbo_frame_tag "remote_modal", class: "flex min-h-0 flex-col bg-neutral-primary-soft" do %>
  <div class="flex shrink-0 items-start justify-between gap-4 border-b border-brand bg-brand px-5 py-4">
    <h4 class="text-lg font-semibold text-white" id="remote_modal_title"><%= title %></h4>

    <button type="button"
            class="-m-1.5 inline-flex h-9 w-9 shrink-0 items-center justify-center rounded-lg text-white transition-colors hover:bg-white/10 hover:text-white focus:ring-2 focus:ring-white/40"
            data-action="modal#dismiss"
            aria-label="Cerrar">
      <svg class="h-3.5 w-3.5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6" />
      </svg>
    </button>
  </div>

  <div class="min-h-0 flex-1 overflow-y-auto bg-neutral-primary-soft px-5 py-4" id="remote_modal_body">
    <%= yield %>
  </div>
<% end %>
