<%= simple_form_for [location_equipment, comment] do |f| %>
  <%= f.input :description, label: "Comentario", as: :text, input_html: { rows: 4 } %>

  <%= f.button :submit, "Aceptar", class: "btn btn-success" %>
<% end %>
