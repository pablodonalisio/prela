<% image = report.new_record? ? nil : report.images[n] %>
<div class="mt-3 border col-sm-6 p-2">
  <h4><%= title %></h4>
  <%= render "shared/file_upload", file: image,
                                    form: f,
                                    resize: [nil, 250],
                                    file_field: :images, new_label: "Imágen",
                                    accepted_formats: "image/jpeg, image/jpg, image/png",
                                    multiple: true %>
</div>