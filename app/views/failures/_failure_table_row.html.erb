<%= content_tag :tr, id: dom_id(failure), class: "border-t border-default" do %>
  <td class="px-4 py-3 align-middle">
    <p class="m-0 text-heading"><%= failure.description.capitalize %></p>
    <p class="m-0 text-xs text-body-subtle">Fecha: <%= failure.date.strftime("%d/%m/%Y") %></p>
  </td>
  <td class="px-4 py-3 align-middle">
    <div class="flex items-center gap-1">
      <% if current_user.admin? %>
        <%= link_to edit_location_equipment_failure_path(location_equipment, failure),
                    class: "inline-flex h-8 w-8 items-center justify-center rounded-lg text-body hover:bg-neutral-tertiary hover:text-heading",
                    data: {turbo_frame: "remote_modal"},
                    aria: {label: "Editar"} do %>
          <%= icon(:edit) %>
        <% end %>
        <%= button_to location_equipment_failure_path(location_equipment, failure),
                      method: :delete,
                      class: "inline-flex h-8 w-8 items-center justify-center rounded-lg text-body hover:bg-red-100 hover:text-red-700 dark:hover:bg-red-900/40",
                      form: {data: {turbo_confirm: "¿Estás seguro?"}},
                      aria: {label: "Eliminar"} do %>
          <%= icon(:trash) %>
        <% end %>
      <% end %>
    </div>
  </td>
<% end %>
