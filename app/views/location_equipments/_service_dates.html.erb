<div class="col-lg-6">
  <% location_equipment.last_service_dates.each do |attribute| %>
    <%= render "last_service_date", label: LocationEquipment.human_attribute_name(attribute),
                                    date: location_equipment.last_service_date(attribute),
                                    location_equipment: location_equipment %>
  <% end %>
  <%= render location_equipment.next_service_dates %>
</div>
