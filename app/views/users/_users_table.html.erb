<tbody id="users">
  <% if users.empty? %>
    <tr id="empty_users">
      <td colspan="4" class="px-4 py-6 text-center text-body-subtle">No hay Usuarios</td>
    </tr>
  <% else %>
    <%= render partial: "users/user_table_row", collection: users, as: :user %>
  <% end %>
</tbody>
